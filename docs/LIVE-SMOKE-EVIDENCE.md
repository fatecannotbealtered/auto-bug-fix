# Live Smoke Evidence

No live Jira/GitLab/Kibana smoke run is recorded in this repository yet.

Current release readiness remains `beta`. To declare `stable`, add a dated, redacted record containing:

- auto-bug-fix version and commit.
- dependency CLI versions for `jira-cli`, `gitlab-cli`, and optional `kibana-cli`.
- sanitized Jira issue key or test fixture identifier.
- sanitized GitLab project path and MR result.
- command transcript limited to JSON envelopes with secrets and internal identifiers removed.
- outcome and any manual intervention required.
